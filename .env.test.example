# Credentials for the Playwright e2e suite. Copy to .env.test.local (gitignored)
# and fill in a THROWAWAY member account on the Supabase project — never an
# admin, and never a real person's login. Without these the suite skips.
TEST_USER_EMAIL=
TEST_USER_PASSWORD=
